/**
 * PayRoll Pro – Auth Layout
 * Centered card layout with gradient background for login/auth pages.
 */

const AuthLayout = ({ children }) => {
  return (
    <div className="auth-page">
      {children}
    </div>
  );
};

export default AuthLayout;
